"""Model-parameter-estimation domain validation.

Notebook-centric: ``backend.py`` holds the model families + estimation infra
(the single source of truth) and ``parameter_estimation.ipynb`` is the rerunnable
report that imports it and writes figures to ``figures/``.
"""
